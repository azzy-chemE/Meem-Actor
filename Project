import java.util.Scanner;

public class Project {
    public static void main(String[] args) {
        // okay - I'm going to go with the batch reactor simulator.

        System.out.println("Helloooo! Nice to meet you! I'm Meemaw, a chemical engineer here to help!");
        System.out.println("I'm going to set up a batch reactor, where a stirred-tank reactor will run and you can see the reactant conversion over time!");
        System.out.println("The reaction is A → B. What is reactant A?");

        Scanner scanner = new Scanner(System.in); // Create Scanner object
        String A = scanner.nextLine(); // Read entire line of input

        System.out.println("Cool! And what is product B you want to get? Please input something possible. I'm too lazy to deal with your mistakes for you, LOL.");
        String B = scanner.nextLine();

        // Example: ask user a Yes/No question
        System.out.println("Do you want to continue? (Yes/No)");
        String choice = scanner.nextLine();
        if (choice.equalsIgnoreCase("Yes") || choice.equalsIgnoreCase("No")) {
            System.out.println("Ok, cool!");
        } else {
            System.out.println("Invalid choice.");
        }
        scanner.close();
    }
}